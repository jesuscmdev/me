{
   "buildCommand": "npm run build",
   "builds": [
      {
         "src": "src",
         "use": "@vercel/node"
      }
   ],
   "routes": [
      {
         "src": "/[^.]+",
         "dest": "/",
         "status": 200
      }
   ]
}